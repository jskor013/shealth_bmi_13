# 프롬프트 기록: cursorrules and report agent

- 보고서 파일: report/01.cursorrules_and_report_agent_report.md
- Export 일시: 2026-05-19T08:07:55Z
- Export 원칙: 요약 없음, 원문 보존 우선
- Transcript 출처: C:\Users\disab\.cursor\projects\e-dev-shealth-bmi-13\agent-transcripts\f4783447-a387-43ae-9a4f-c55f0f6d3f86\f4783447-a387-43ae-9a4f-c55f0f6d3f86.jsonl

## 대화 전문

### Turn 1 - User Prompt

[P] 당신은 레거시 코드 QA/리팩토링을 돕는 시니어 C++ 엔지니어입니다.
[C] 이 C++17 프로젝트에서 Cursor AI가 항상 따라야 하는 규칙을
    프로젝트 루트의 .cursorrules로 작성하려고 합니다.
[T] 아래 요구를 만족하는 .cursorrules 내용을 작성해줘.
    - 기술 스택: C++17 + CMake + Google Test (+ gcov/lcov)
    - 절대 규칙: 
    - 테스트 규칙: Given-When-Then, TEST_F, 경계값포함
    - 리팩토링 규칙: 테스트 Green 상태에서만 진행, 매직 넘버 상수화 권장
[F] .cursorrules 파일에 그대로 붙여넣을 수 있는 텍스트

### Turn 1 - AI Answer

프로젝트 구조와 기존 규칙을 확인한 뒤 `.cursorrules` 초안을 작성합니다.

[REDACTED]

[Tool: Read]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\README.md"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "**/*",
  "target_directory": "e:\\dev\\shealth_bmi_13"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "**/.cursorrules",
  "target_directory": "e:\\dev\\shealth_bmi_13"
}
```


### Turn 1 - AI Answer (continued)

[REDACTED]

[Tool: Read]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\CMakeLists.txt"
}
```


[Tool: Read]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\src\\test\\cpp\\SHealthBMITest.cpp"
}
```


[Tool: Read]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\src\\main\\cpp\\SHealth.h"
}
```


[Tool: Read]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\TDD 기반 암호 검사기 또는 BMI 프로젝트용 프롬프트.txt"
}
```


### Turn 1 - AI Answer (continued)

[REDACTED]

[Tool: Read]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\src\\main\\cpp\\SHealth.cpp",
  "limit": 80
}
```


### Turn 1 - AI Answer (continued)

프로젝트 구조와 도메인(BMI 임계값, 연령대)을 반영해 `.cursorrules` 초안을 작성합니다.

[REDACTED]

[Tool: Write]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\.cursorrules",
  "contents": "# SHealth BMI — Cursor AI 규칙 (C++17 레거시 QA / 리팩토링)\n\n당신은 이 저장소의 **시니어 C++ 엔지니어**입니다. 레거시 코드 분석, 테스트 보강, 안전한 리팩토링을 돕습니다.\n\n---\n\n## 기술 스택\n\n- **언어**: C++17 (`CMAKE_CXX_STANDARD 17`, 표준 이상 기능·비표준 확장 금지)\n- **빌드**: CMake 3.10+\n- **테스트**: Google Test (FetchContent `v1.14.0`, `gtest_discover_tests`)\n- **커버리지**(선택): gcov + lcov (`--coverage`, `lcov --capture`, `genhtml`)\n\n### 프로젝트 레이아웃\n\n```\nCMakeLists.txt\nsrc/main/cpp/   — SHealth.h, SHealth.cpp, SHealthBMI.cpp (도메인·진입점)\nsrc/test/cpp/   — SHealthBMITest.cpp (단위 테스트)\nshealth.dat     — 샘플 입력 (id,age,weight,height)\n```\n\n- 프로덕션 로직은 `shealth_lib`에 두고, 테스트·실행 파일은 이를 링크한다.\n- `main`은 `SHealthBMI.cpp`에만 둔다. 테스트 가능한 로직은 `SHealth` 또는 분리된 클래스/함수로 유지한다.\n\n### 도메인 상수 (README 기준 — 변경 시 테스트·문서 동시 갱신)\n\n| 항목 | 값 |\n|------|-----|\n| BMI | `weight(kg) / (height(m))²`, height는 cm → m 변환 |\n| 저체중 | BMI ≤ 18.5 |\n| 정상체중 | 18.5 < BMI < 23 |\n| 과체중 | 23 ≤ BMI < 25 |\n| 비만 | BMI ≥ 25 |\n| 연령대 | 20대~70대 (10년 단위: `[a, a+10)`) |\n| 누락 보정 | `weight == 0` → 동일 연령대 평균 체중 적용 |\n\n---\n\n## 절대 규칙\n\n1. **동작 변경 금지(테스트 없이)**: 리팩토링·스타일 수정은 기존 테스트가 전부 통과(Green)한 뒤에만 진행한다.\n2. **최소 변경**: 요청 범위 밖 파일·공개 API·출력 형식을 임의로 바꾸지 않는다.\n3. **빌드·테스트 검증**: 변경 후 반드시 `cmake --build .` 및 `ctest`(또는 `SHealthBMITest` 실행)로 확인한다. 실패 시 원인을 수정한 뒤 완료를 보고한다.\n4. **헤더/구현 분리**: 선언은 `.h`, 정의는 `.cpp`. 순환 의존을 만들지 않는다.\n5. **전역 상태 금지**: 전역 변수·매크로 상수 남용을 제거·추가하지 않는다. 설정값은 `namespace` 내 `constexpr` 또는 클래스 멤버로 둔다.\n6. **비밀·대용량 데이터**: `.env`, 자격 증명, 개인 실데이터를 커밋·로그에 넣지 않는다. 테스트는 고정 픽스처·임시 파일을 사용한다.\n7. **STL 우선**: 동일 동작이면 C 스타일 배열·수동 버퍼 대신 `std::vector`, `std::string`, 알고리즘(`<algorithm>`)을 사용한다(README 허용).\n8. **한 번에 하나의 목표**: “테스트 추가 → Green → 리팩토링 → Green” 순서를 지킨다. 테스트와 대규모 리팩토링을 한 커밋/한 패치에 섞지 않는다.\n\n---\n\n## 테스트 규칙\n\n### 구조\n\n- **프레임워크**: Google Test (`#include <gtest/gtest.h>`).\n- **픽스처**: 상태를 공유하는 테스트는 `TEST_F(FixtureName, TestName)` + `class FixtureName : public ::testing::Test`를 사용한다. 단순 순수 함수는 `TEST`도 허용한다.\n- **Given-When-Then**: 각 테스트 본문에 주석으로 구분한다.\n\n```cpp\nTEST_F(SHealthTest, ClassifiesNormalWeightAtBoundary) {\n    // Given: BMI가 정상 범위 하한 직후인 사용자\n    // When:  classifyBmi(...) 호출\n    // Then:  Category::Normal 반환\n}\n```\n\n### 네이밍·범위\n\n- 테스트 이름: `동작_조건_기대결과` (영문 PascalCase/camelCase 혼용 가능, 의미는 명확히).\n- **한 테스트 = 한 가지 검증**. 여러 `EXPECT`는 같은 행위·결과에 한정한다.\n\n### 반드시 다룰 영역\n\n1. BMI 계산 (키 cm, 몸무게 kg)\n2. 연령대 평균 체중 보정 (`weight == 0`)\n3. BMI 카테고리 분류 (저체중/정상/과체중/비만)\n4. 연령대별 통계·비율 (`getBmiRatio` 등)\n5. 예외·오류: 파일 없음, 빈 입력, 잘못된 CSV, `height == 0`(기능 추가 시 평균 보정)\n\n### 경계값 (Boundary)\n\n다음은 **반드시** 명시적 테스트 케이스로 포함한다.\n\n| 경계 | 포함할 값 |\n|------|-----------|\n| BMI 분류 | `18.5`, `18.5+ε`, `23`, `25`, 각 구간 내부 대표값 |\n| 연령대 | `19/20`, `29/30`, …, `69/70`, 구간 경계 `a`, `a+10-ε` |\n| 누락 | `weight == 0`, `weight > 0`, 보정 후 BMI 변화 |\n| 키 | `height == 0`(정의된 동작), 매우 작은/큰 값 |\n| 비율 | 분모 0(해당 연령대 인원 없음) |\n\n### TDD 흐름\n\n1. 실패하는 테스트 먼저 작성(Red)\n2. 최소 구현으로 통과(Green)\n3. 리팩토링(아래 규칙) 후 다시 Green\n\n### 커버리지 (gcov/lcov)\n\n- CMake에 `--coverage` 플래그 추가 시, 변경한 `.cpp`의 분기·경계가 커버되는지 확인한다.\n- 커버리지만 올리는 의미 없는 테스트는 추가하지 않는다.\n\n---\n\n## 리팩토링 규칙\n\n1. **Green에서만**: `ctest` 전체 통과 후에만 구조 변경·이름 변경·추출·SRP 분리를 한다.\n2. **매직 넘버 상수화**: `18.5`, `23`, `25`, `100.0`(cm→m), 연령 `20`/`70`/`10` 등은 이름 있는 `constexpr` 또는 `enum class`로 모은다. 도메인 표와 일치시킨다.\n3. **SRP·DRY**: 한 함수는 한 가지 일만. 중복 루프·분기는 함수/작은 클래스로 추출한다.\n4. **네이밍**: 축약·불명확 이름(`sum`, `a`, `type`만 단독 사용)을 도메인 용어로 바꾼다 (`ageGroup`, `BmiCategory`).\n5. **하드코드·전역 제거**: 연령대별 `underweight20`… 반복 필드는 `std::map`/`std::array`/구조체 벡터 등으로 일반화를 검토한다(동작 동일 유지).\n6. **리팩토링 단위**: 네이밍 → 상수 추출 → 함수 추출 → 책임 분리 순으로 작은 단계; 각 단계마다 테스트 실행.\n7. **기능 추가**(README 4단계): 테스트 먼저 → 구현 → 리팩토링. Height=0 보정, BMI 분포 비율, 정상 범위 사용자 목록 등은 기존 API를 깨지 않도록 확장한다.\n\n---\n\n## 코드 스타일 (C++17)\n\n- `const`·`constexpr` 적극 사용; 가능하면 `const&` 전달.\n- RAW `new`/`delete` 지양; 스마트 포인터는 소유권이 불명확할 때만.\n- 예외: 프로젝트가 이미 `cerr`+반환 코드 패턴이면 그 스타일을 유지하고, 새 코드에서만 일관되게 선택한다.\n- `#pragma once` 사용(기존 헤더와 동일).\n- include 순서: 해당 헤더 → C++ 표준 → 서드파티 → 프로젝트.\n\n---\n\n## 빌드·실행 명령 (참고)\n\n```bash\nmkdir -p build && cd build\ncmake ..\ncmake --build .\nctest --output-on-failure\n./SHealthBMI    # Windows: SHealthBMI.exe\n```\n\n커버리지 예시:\n\n```bash\ncmake .. -DCMAKE_CXX_FLAGS=\"--coverage -O0 -g\"\ncmake --build .\nctest\nlcov --capture --directory . --output-file coverage.info\ngenhtml coverage.info --output-directory coverage_html\n```\n\n---\n\n## AI 작업 시 보고 형식\n\n변경 제안·완료 시 간단히:\n\n1. **무엇을** 바꿨는지(파일 목록)\n2. **왜** 바꿨는지(테스트·스멜·요구사항)\n3. **검증**: 빌드/테스트/커버리지 결과(실행 못 하면 그 사실 명시)\n\n사용자가 커밋을 요청하기 전까지 `git commit`은 하지 않는다.\n"
}
```


### Turn 1 - AI Answer (continued)

`.cursorrules` 파일을 프로젝트 루트에 생성했습니다. 아래 내용을 그대로 복사해 사용해도 됩니다.

---

**구성 요약**

| 섹션 | 내용 |
|------|------|
| 기술 스택 | C++17, CMake, GTest, gcov/lcov, 디렉터리·도메인 상수 표 |
| 절대 규칙 | Green 전 리팩토링 금지, 최소 변경, 빌드/ctest 검증, 전역 상태 금지, STL 우선 등 8항 |
| 테스트 규칙 | `TEST_F`, Given-When-Then 주석, 경계값 표(18.5/23/25, 연령대, weight=0), TDD Red→Green |
| 리팩토링 규칙 | Green 후만, 매직 넘버 `constexpr`화, SRP/DRY, 단계별 작은 리팩토링 |

**도메인 반영**: README의 BMI 구간, 연령대 `[a, a+10)`, `weight==0` 보정, `shealth_lib` / `src/main/cpp` · `src/test/cpp` 구조를 규칙에 명시했습니다.

**절대 규칙**은 요청에 빈 항목이 있어, 레거시 QA 실습(README Activities 1~4)에 맞는 8가지를 채웠습니다. 빠진 항목이 있으면 알려주시면 반영하겠습니다.

파일 위치: `e:\dev\shealth_bmi_13\.cursorrules`

[REDACTED]

### Turn 2 - User Prompt

보고서 작성 및 기록, 깃 허브를 관리하는 Agent를 만들어줘.
.cursor/agent 에 생성
보고서작성 및 백업 명령을 받으면
[1] 작업한 내용에 대한 보고서 작성 : report 폴더에 xx(숫자 01부터 시작).적절한 보고서 이름_report.md 파일로 작성
[2] 작업한 모든 대화내용을 요약없이 Export하여 저장 : prompting 폴더에 xx(report 숫자와 동일).적절한 보고서 이름_prompt.md
[3] 깃허브에 모든 작업내용 올리기.
이상 3단계의 명령을 루틴하게 수행하는 Agent야.

### Turn 2 - AI Answer

Cursor Agent 설정 방식과 프로젝트 구조를 확인한 뒤 `.cursor/agent`에 보고·백업·GitHub 루틴 에이전트를 생성합니다.

[REDACTED]

[Tool: Glob]
```json
{
  "glob_pattern": "**/.cursor/**/*",
  "target_directory": "e:\\dev\\shealth_bmi_13"
}
```


[Tool: Read]
```json
{
  "path": "C:\\Users\\disab\\.cursor\\skills-cursor\\create-skill\\SKILL.md"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "**/agent*",
  "target_directory": "C:\\Users\\disab\\.cursor"
}
```


### Turn 2 - AI Answer (continued)

[REDACTED]

[Tool: Read]
```json
{
  "path": "C:\\Users\\disab\\.cursor\\skills-cursor\\cursor-guide\\SKILL.md",
  "limit": 150
}
```


[Tool: Grep]
```json
{
  "pattern": "\\.cursor/agent",
  "path": "C:\\Users\\disab\\.cursor"
}
```


[Tool: WebSearch]
```json
{
  "search_term": "Cursor IDE .cursor/agents custom agent configuration 2025 2026",
  "explanation": "Find how Cursor project agents are defined under .cursor/agent or .cursor/agents"
}
```


### Turn 2 - AI Answer (continued)

[REDACTED]

[Tool: WebFetch]
```json
{
  "url": "https://cursor.com/docs/agent/subagents"
}
```


[Tool: WebFetch]
```json
{
  "url": "https://cursor.com/learn/customizing-agents"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "**/.cursor/**/*",
  "target_directory": "e:\\dev"
}
```


### Turn 2 - AI Answer (continued)

[REDACTED]

[Tool: Read]
```json
{
  "path": "e:\\dev\\TDD_TV_13\\.cursor\\agent\\report-backup-agent.md"
}
```


[Tool: Read]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\.gitignore"
}
```


[Tool: Shell]
```json
{
  "command": "cd \"e:\\dev\\shealth_bmi_13\" && git branch -a && git remote -v 2>&1",
  "description": "Check git branches and remotes"
}
```


### Turn 2 - AI Answer (continued)

[REDACTED]

[Tool: Shell]
```json
{
  "command": "cd \"e:\\dev\\shealth_bmi_13\"; git branch -a; git remote -v",
  "description": "Check git branches and remotes (PowerShell)"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "report/**",
  "target_directory": "e:\\dev\\shealth_bmi_13"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "prompting/**",
  "target_directory": "e:\\dev\\shealth_bmi_13"
}
```


### Turn 2 - AI Answer (continued)

기존 `TDD_TV_13` 패턴을 참고해 SHealth BMI 프로젝트용 보고·백업 Agent를 생성합니다.

[REDACTED]

[Tool: Write]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\.cursor\\agent\\report-backup-agent.md",
  "contents": "---\nname: report-backup\ndescription: >-\n  Writes work reports, exports full conversation transcripts without summarization,\n  and pushes all changes to GitHub. Use when the user says 보고서작성 및 백업,\n  보고서 작성 및 백업, 작업 보고서 만들고 GitHub에 올려줘, or similar backup commands.\nmodel: inherit\n---\n\n# Report And Backup Agent\n\n이 Agent는 사용자가 **보고서작성 및 백업** 또는 같은 의미의 명령을 내렸을 때만 실행한다.  \n목표는 **(1) 작업 보고서 작성 → (2) 대화 전문 Export → (3) GitHub 업로드**를 항상 같은 순서로 수행하는 것이다.\n\n## 역할\n\n- **SHealth BMI** 프로젝트(C++17, CMake, Google Test, gcov/lcov)의 작업 기록·백업 담당 Agent다.\n- `report/`, `prompting/` 폴더에 산출물을 남기고, 코드·문서·기록을 GitHub 원격 저장소에 올린다.\n- 프로젝트 규칙은 루트 `.cursorrules`를 따른다.\n\n## 실행 트리거\n\n다음과 같은 요청을 받으면 이 3단계 루틴을 **순서대로** 수행한다.\n\n- `보고서작성 및 백업`\n- `보고서 작성 및 백업`\n- `보고서 쓰고 백업해줘`\n- `작업 내용 보고서 만들고 GitHub에 올려줘`\n- `/report-backup` (명시적 호출)\n\n## 사전 확인\n\n1. `git status`로 현재 변경 사항을 확인한다.\n2. `report/` 폴더의 기존 파일명을 나열해 **다음 보고서 번호**를 정한다.\n3. `report/`, `prompting/` 폴더가 없으면 생성한다.\n4. 번호는 `01`부터 시작하고, 기존 `NN.` 접두사 중 **최대 번호 + 1**을 사용한다. (없으면 `01`)\n5. 보고서 이름(`적절한_보고서_이름`)은 이번 작업의 핵심을 짧은 **snake_case 영문**으로 정한다.\n\n파일명 예:\n\n```text\nreport/01.cursorrules-setup_report.md\nprompting/01.cursorrules-setup_prompt.md\n```\n\n---\n\n## 1단계: 작업 보고서 작성\n\n`report/` 폴더에 다음 형식으로 작성한다.\n\n**파일명:** `xx.적절한_보고서_이름_report.md`\n\n**필수 포함 항목:**\n\n```markdown\n# 작업 보고서: <작업 이름>\n\n## 개요\n\n- 작업 일시: (ISO 8601 또는 로컬 날짜)\n- 작업 목적:\n- 핵심 결과:\n\n## 변경 내용\n\n- 수정/추가/삭제한 파일:\n- 주요 구현·리팩토링 내용:\n- 테스트 변경 내용:\n\n## 검증\n\n- 실행한 빌드 명령:\n- 실행한 테스트 명령:\n- 테스트 결과: (Green/Red, 통과·실패 수)\n- 커버리지: (gcov/lcov 실행 시에만)\n\n## 도메인·품질 메모 (해당 시)\n\n- BMI 계산·분류·연령대 통계 관련 변경:\n- `.cursorrules` / Agent / 문서 변경:\n\n## GitHub 업로드\n\n- 브랜치:\n- 커밋 해시:\n- 커밋 메시지:\n- 원격 저장소 URL:\n- Push 결과:\n\n## 주의 사항\n\n- 남은 리스크:\n- 후속 작업:\n```\n\n보고서는 **요약 가능**하나, 사실 관계(파일 경로, 명령, 결과)는 정확히 기록한다.\n\n---\n\n## 2단계: 전체 대화 내용 Export (요약 금지)\n\n`prompting/` 폴더에 **보고서와 동일한 번호·동일한 작업 이름**으로 저장한다.\n\n**파일명:** `xx.적절한_보고서_이름_prompt.md`\n\n### 작성 원칙 (엄격)\n\n- 이번 세션(또는 지정된 작업 범위)의 **사용자 Prompt와 AI 답변을 요약하지 않고 전문**으로 Export한다.\n- 사용자 Prompt는 **원문 그대로** 보존한다.\n- AI 답변은 사용자에게 표시된 **최종 답변**과 **중간 진행 메시지**를 가능한 한 누락 없이 보존한다.\n- 실행한 **셸 명령**, **테스트 결과**, **중요한 도구 출력**은 시간순으로 원문에 가깝게 기록한다.\n- 출력이 매우 길어도 **임의 요약하지 않는다**. 시스템 한계로 일부만 넣을 수 있을 때는 생략 구간과 이유를 명시한다.\n- **숨겨진 시스템 메시지**, **비공개 내부 추론(thinking)** 은 기록하지 않는다.\n\n### 기본 형식\n\n```markdown\n# 프롬프트 기록: <작업 이름>\n\n- 보고서 파일: report/xx.적절한_보고서_이름_report.md\n- Export 일시:\n- Export 원칙: 요약 없음, 원문 보존 우선\n\n## 대화 전문\n\n### Turn 1 - User Prompt\n\n<사용자 Prompt 원문 전체>\n\n### Turn 1 - AI Answer\n\n<AI 답변 원문 전체>\n\n### Turn 2 - User Prompt\n\n...\n\n### Turn N - AI Answer\n\n...\n\n## 실행 명령 및 도구 출력\n\n<명령·stdout/stderr·주요 도구 출력 원문, 시간순>\n\n## 최종 상태\n\n<완료 여부, 생성된 report/prompting 경로, 커밋·push 예정 또는 완료>\n```\n\n---\n\n## 3단계: GitHub에 모든 작업 내용 업로드\n\n1·2단계 파일 작성 **이후** 다음을 수행한다.\n\n1. `git status`로 변경 파일을 확인한다.\n2. 코드 변경이 포함된 경우: `build`에서 `cmake --build .` 및 `ctest --output-on-failure` 실행.\n3. 테스트가 **Red**이면 commit/push **중단**하고 실패 원인을 사용자에게 보고한다.\n4. `.env`, credential, token, key 등 **민감 정보**가 staging에 없는지 확인한다.\n5. 작업 파일 + `report/xx...._report.md` + `prompting/xx...._prompt.md` (+ 기타 변경)을 staging 한다.\n6. 작업 내용을 설명하는 커밋 메시지로 **commit** 한다.\n7. upstream이 있으면 `git push`; 없으면 `git push -u origin HEAD`.\n8. push 결과·커밋 해시를 **1단계 보고서의「GitHub 업로드」섹션에 반영**한다. (이미 작성했다면 파일을 수정해 갱신)\n\n### 커밋 메시지 예\n\n```text\nAdd report and prompt backup for <work-name>\n```\n\n```text\nImplement <feature-or-fix> with report backup\n```\n\n### Git 안전 규칙\n\n- **보고서작성 및 백업** 명령 시, 해당 루틴에 필요한 **commit·push는 사용자 승인으로 간주**한다.\n- `git reset --hard`, **force push**, rebase, amend는 사용자가 **명시적으로 요청할 때만** 수행한다.\n- `main`/`master`에 force push하지 않는다.\n- 기존 사용자 변경사항을 임의로 되돌리지 않는다.\n- `git config`는 변경하지 않는다.\n\n### 원격 저장소 (이 프로젝트)\n\n- remote: `origin` → `https://github.com/jskor013/shealth_bmi_13.git`\n- 일반적으로 현재 작업 브랜치(`prompting` 등)에 push한다. 브랜치 변경이 필요하면 사용자에게 확인한다.\n\n---\n\n## 완료 보고\n\n3단계가 끝나면 사용자에게 다음을 간단히 알린다.\n\n1. 생성된 보고서 경로 (`report/xx...._report.md`)\n2. 생성된 프롬프트 기록 경로 (`prompting/xx...._prompt.md`)\n3. 커밋 해시·브랜치·push 성공 여부\n4. 테스트/빌드 결과 (실행한 경우)\n\n---\n\n## 실패 시\n\n| 상황 | 동작 |\n|------|------|\n| 테스트 Red | 3단계 중단, 보고서·prompting은 저장 가능, push 하지 않음 |\n| push 거부/충돌 | 원인 보고, force push 없이 해결 방법 제안 |\n| 대화 Export 불완전 | 생략 구간·이유를 prompting 파일에 명시 |\n"
}
```


[Tool: Write]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\report\\.gitkeep",
  "contents": ""
}
```


[Tool: Write]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\prompting\\.gitkeep",
  "contents": ""
}
```


### Turn 2 - AI Answer (continued)

`.cursor/agent`에 **보고서 작성·대화 백업·GitHub 업로드** 3단계 루틴 Agent를 추가했습니다.

## 생성된 파일

| 경로 | 설명 |
|------|------|
| `.cursor/agent/report-backup-agent.md` | Agent 정의 (트리거, 1~3단계, 파일명 규칙, Git 안전 규칙) |
| `report/.gitkeep` | 보고서 저장 폴더 |
| `prompting/.gitkeep` | 대화 Export 저장 폴더 |

## 사용 방법

다음처럼 요청하면 Agent가 **항상 같은 순서**로 실행합니다.

- `보고서작성 및 백업`
- `보고서 작성 및 백업`
- `/report-backup` (명시적 호출)

## 3단계 루틴

1. **보고서** → `report/01.작업이름_report.md` (번호는 기존 최대+1, `01`부터)
2. **대화 Export** → `prompting/01.작업이름_prompt.md` (동일 번호·이름, **요약 없이** 원문)
3. **GitHub** → staging → commit → `git push` (테스트 Red면 push 중단)

## 참고

- `TDD_TV_13`의 동일 Agent 패턴을 따랐고, SHealth BMI용으로 `.cursorrules`, BMI 도메인, 원격 `origin`(jskor013/shealth_bmi_13)을 반영했습니다.
- Cursor 공식 **Subagent** 경로는 `.cursor/agents/`(복수)입니다. 현재는 요청하신 `.cursor/agent/`(단수)에 두었습니다. Task 도구로 자동 위임까지 쓰려면 같은 파일을 `.cursor/agents/report-backup.md`로 복사해 두면 됩니다.

첫 백업을 지금 실행할까요? (`보고서작성 및 백업`이라고 보내주시면 됩니다.)

[REDACTED]

### Turn 3 - User Prompt

보고서 작성 및 기록, 깃 허브를 관리하는 Agent를 만들어줘.
.cursor/agents 에 생성
보고서작성 및 백업 명령을 받으면
[1] 작업한 내용에 대한 보고서 작성 : report 폴더에 xx(숫자 01부터 시작).적절한 보고서 이름_report.md 파일로 작성
[2] 작업한 모든 대화내용을 요약없이 Export하여 저장 : prompting 폴더에 xx(report 숫자와 동일).적절한 보고서 이름_prompt.md
[3] 깃허브에 모든 작업내용 올리기.
이상 3단계의 명령을 루틴하게 수행하는 Agent야.

### Turn 3 - AI Answer

Cursor Agent 형식과 프로젝트 구조를 확인한 뒤 `.cursor/agents`에 보고서·백업·GitHub 관리 Agent를 생성합니다.

[REDACTED]

[Tool: Read]
```json
{
  "path": "C:\\Users\\disab\\.cursor\\skills-cursor\\create-skill\\SKILL.md"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "**/.cursor/**/*",
  "target_directory": "e:\\dev\\shealth_bmi_13"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "**/agents/**",
  "target_directory": "C:\\Users\\disab\\.cursor"
}
```


### Turn 3 - AI Answer (continued)

[REDACTED]

[Tool: Grep]
```json
{
  "pattern": "\\.cursor/agents",
  "path": "C:\\Users\\disab\\.cursor"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "**/*.agent.md",
  "target_directory": "C:\\Users\\disab\\.cursor"
}
```


[Tool: Read]
```json
{
  "path": "C:\\Users\\disab\\.cursor\\skills-cursor\\create-rule\\SKILL.md",
  "limit": 100
}
```


### Turn 3 - AI Answer (continued)

[REDACTED]

[Tool: Read]
```json
{
  "path": "C:\\Users\\disab\\.cursor\\extensions\\vscjava.vscode-java-debug-0.59.0-universal\\bundled\\agents\\debug.agent.md"
}
```


[Tool: Read]
```json
{
  "path": "C:\\Users\\disab\\.cursor\\extensions\\vscjava.vscode-java-debug-0.59.0-universal\\bundled\\agents\\README.md"
}
```


[Tool: WebSearch]
```json
{
  "search_term": "Cursor IDE .cursor/agents custom agent md format 2025",
  "explanation": "Find official Cursor custom agent file format for .cursor/agents directory."
}
```


### Turn 3 - AI Answer (continued)

[REDACTED]

[Tool: WebFetch]
```json
{
  "url": "https://cursor.com/docs/agent/subagents"
}
```


[Tool: Read]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\.gitignore"
}
```


[Tool: Shell]
```json
{
  "command": "cd \"e:\\dev\\shealth_bmi_13\" && git branch -a && git remote -v && git status",
  "description": "Check git branches and remote config"
}
```


### Turn 3 - AI Answer (continued)

[REDACTED]

[Tool: Shell]
```json
{
  "command": "cd \"e:\\dev\\shealth_bmi_13\"; git branch -a; git remote -v; git status",
  "description": "Check git branches and remote"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "**/report/**",
  "target_directory": "e:\\dev\\shealth_bmi_13"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "**/prompting/**",
  "target_directory": "e:\\dev\\shealth_bmi_13"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "**/*",
  "target_directory": "C:\\Users\\disab\\.cursor\\projects\\e-dev-shealth-bmi-13\\agent-transcripts"
}
```


### Turn 3 - AI Answer (continued)

[REDACTED]

[Tool: Read]
```json
{
  "path": "C:\\Users\\disab\\.cursor\\projects\\e-dev-shealth-bmi-13\\agent-transcripts\\f4783447-a387-43ae-9a4f-c55f0f6d3f86\\f4783447-a387-43ae-9a4f-c55f0f6d3f86.jsonl",
  "limit": 30
}
```


[Tool: Read]
```json
{
  "path": "C:\\Users\\disab\\.cursor\\skills-cursor\\create-hook\\SKILL.md",
  "limit": 80
}
```


### Turn 3 - AI Answer (continued)

[REDACTED]

[Tool: Glob]
```json
{
  "glob_pattern": "**/.cursor/**/*",
  "target_directory": "e:\\dev\\shealth_bmi_13"
}
```


[Tool: Read]
```json
{
  "path": "e:\\dev\\TDD_TV_13\\.cursor\\agent\\report-backup-agent.md",
  "limit": 50
}
```


### Turn 3 - AI Answer (continued)

[REDACTED]

[Tool: Shell]
```json
{
  "command": "Get-ChildItem -Force \"e:\\dev\\shealth_bmi_13\\.cursor\" -Recurse -ErrorAction SilentlyContinue | Select-Object FullName",
  "description": "List .cursor directory contents"
}
```


[Tool: Read]
```json
{
  "path": "e:\\dev\\TDD_TV_13\\.cursor\\agents\\report-backup.md"
}
```


### Turn 3 - AI Answer (continued)

[REDACTED]

[Tool: Read]
```json
{
  "path": "e:\\dev\\TDD_TV_13\\.cursor\\agent\\report-backup-agent.md"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "**/*",
  "target_directory": "e:\\dev\\TDD_TV_13\\.cursor"
}
```


### Turn 3 - AI Answer (continued)

`.cursor/agents`에 Cursor Subagent 형식으로 Agent를 생성하고, 대화 Export용 보조 스크립트와 `report`/`prompting` 폴더를 추가합니다.

[REDACTED]

[Tool: Write]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\.cursor\\agents\\report-backup.md",
  "contents": "---\nname: report-backup\ndescription: >-\n  Writes work reports to report/, exports full conversation transcripts without\n  summarization to prompting/, and pushes all changes to GitHub. Use when the user\n  says 보고서작성 및 백업, 보고서 작성 및 백업, 작업 보고서 만들고 GitHub에 올려줘,\n  or /report-backup.\nmodel: inherit\n---\n\n# Report And Backup Agent\n\n사용자가 **보고서작성 및 백업** 또는 동일한 의미의 명령을 내리면, 아래 **3단계를 항상 같은 순서**로 수행한다.\n\n1. `report/xx.작업이름_report.md` — 작업 보고서 작성  \n2. `prompting/xx.작업이름_prompt.md` — 대화 전문 Export (**요약 금지**)  \n3. GitHub — staging → commit → push  \n\n---\n\n## 역할\n\n- **SHealth BMI** 프로젝트(C++17, CMake, Google Test, gcov/lcov)의 작업 기록·백업 담당 Subagent.\n- 프로젝트 코딩 규칙은 루트 `.cursorrules`를 따른다.\n- Subagent는 부모 대화 맥락이 없을 수 있으므로, 부모가 넘긴 작업 요약·변경 파일 목록을 우선 사용하고, 부족하면 `git status`, `git diff`, 소스 파일을 직접 확인한다.\n\n## 실행 트리거\n\n- `보고서작성 및 백업`\n- `보고서 작성 및 백업`\n- `보고서 쓰고 백업해줘`\n- `작업 내용 보고서 만들고 GitHub에 올려줘`\n- `/report-backup`\n\n---\n\n## 사전 확인 (모든 단계 전)\n\n1. 프로젝트 루트에서 `git status` 실행.\n2. `report/` 목록을 확인해 **다음 보고서 번호** 결정:\n   - 파일명 패턴: `NN.이름_report.md` (`NN` = 두 자리 숫자)\n   - 기존 최대 `NN` + 1, 없으면 `01`\n3. `report/`, `prompting/` 폴더가 없으면 생성.\n4. **작업 이름** (`적절한_보고서_이름`): 이번 작업 핵심을 짧은 **snake_case 영문**으로 정한다.\n\n파일명 예:\n\n```text\nreport/01.cursorrules_setup_report.md\nprompting/01.cursorrules_setup_prompt.md\n```\n\n---\n\n## 1단계: 작업 보고서 작성\n\n**경로:** `report/xx.적절한_보고서_이름_report.md`\n\n**템플릿:**\n\n```markdown\n# 작업 보고서: <작업 이름>\n\n## 개요\n\n- 작업 일시:\n- 작업 목적:\n- 핵심 결과:\n\n## 변경 내용\n\n- 수정/추가/삭제한 파일:\n- 주요 구현·리팩토링 내용:\n- 테스트 변경 내용:\n\n## 검증\n\n- 실행한 빌드 명령:\n- 실행한 테스트 명령:\n- 테스트 결과: (Green/Red, 통과·실패 수)\n- 커버리지: (gcov/lcov 실행 시에만)\n\n## 도메인·품질 메모 (해당 시)\n\n- BMI 계산·분류·연령대 통계 관련 변경:\n- `.cursorrules` / Agent / 문서 변경:\n\n## GitHub 업로드\n\n- 브랜치:\n- 커밋 해시: (3단계 후 채움)\n- 커밋 메시지:\n- 원격 저장소 URL:\n- Push 결과: (3단계 후 채움)\n\n## 주의 사항\n\n- 남은 리스크:\n- 후속 작업:\n```\n\n보고서는 **사실(경로·명령·결과)을 정확히** 기록한다. 해석·요약은 개요·핵심 결과에만 허용한다.\n\n---\n\n## 2단계: 전체 대화 Export (요약 금지)\n\n**경로:** `prompting/xx.적절한_보고서_이름_prompt.md`  \n(보고서와 **동일한 `xx`·동일한 작업 이름**)\n\n### Export 원칙 (엄격)\n\n- 사용자 Prompt와 AI 답변을 **요약하지 않고 전문**으로 보존한다.\n- 사용자 Prompt는 **원문 그대로**.\n- AI 답변·중간 진행 메시지·도구 호출·셸 출력을 **가능한 한 누락 없이** 시간순 기록.\n- 출력이 길어도 **임의 요약하지 않는다**. 시스템 한계로 생략 시 `<!-- 생략: 이유 -->`로 구간·이유 명시.\n- **thinking**, 숨겨진 시스템 메시지, 비공개 내부 추론은 제외.\n\n### Export 수집 순서\n\n1. **부모 Agent가 전달한 대화 전문**이 있으면 그것을 최우선 사용.\n2. 없으면 Cursor agent transcript JSONL을 읽는다:\n   - Windows: `C:\\Users\\disab\\.cursor\\projects\\e-dev-shealth-bmi-13\\agent-transcripts\\**\\*.jsonl`\n   - **가장 최근 수정** 파일을 기본 사용. 사용자가 세션 ID를 지정하면 해당 파일.\n3. 보조 스크립트 실행 (권장):\n\n```powershell\ncd <프로젝트_루트>\npython .cursor/agents/scripts/export-transcript.py `\n  --number 01 `\n  --slug cursorrules_setup `\n  --output prompting/01.cursorrules_setup_prompt.md\n```\n\n스크립트는 JSONL을 파싱해 Turn 형식 Markdown을 생성한다. 스크립트 후에도 누락 Turn이 있으면 수동 보완한다.\n\n### 출력 형식\n\n```markdown\n# 프롬프트 기록: <작업 이름>\n\n- 보고서 파일: report/xx.적절한_보고서_이름_report.md\n- Export 일시:\n- Export 원칙: 요약 없음, 원문 보존 우선\n- Transcript 출처: (부모 전달 / agent-transcripts 경로)\n\n## 대화 전문\n\n### Turn 1 - User Prompt\n\n<원문 전체>\n\n### Turn 1 - AI Answer\n\n<원문 전체>\n\n### Turn 2 - User Prompt\n\n...\n\n## 실행 명령 및 도구 출력\n\n<명령·stdout/stderr·도구 출력 원문, 시간순>\n\n## 최종 상태\n\n<완료 여부, 생성 파일 경로, 커밋·push 예정 또는 완료>\n```\n\n---\n\n## 3단계: GitHub에 모든 작업 내용 업로드\n\n1·2단계 **완료 후** 수행.\n\n1. `git status`로 변경 파일 확인.\n2. **코드 변경이 포함된 경우** (선택이 아니라 권장):\n   ```powershell\n   if (-not (Test-Path build)) { mkdir build }\n   cd build\n   cmake ..\n   cmake --build .\n   ctest --output-on-failure\n   cd ..\n   ```\n3. 테스트 **Red** → commit/push **중단**, 실패 원인 보고. (보고서·prompting 파일은 이미 저장됨)\n4. `.env`, credential, token, key 등 **민감 정보** staging 여부 확인.\n5. 작업 파일 + `report/xx...._report.md` + `prompting/xx...._prompt.md` + 기타 변경 staging.\n6. 커밋 메시지로 **commit** (보고서작성 및 백업 명령 시 commit·push는 **승인된 것**으로 간주).\n7. `git push` 또는 upstream 없으면 `git push -u origin HEAD`.\n8. 커밋 해시·push 결과를 **1단계 보고서「GitHub 업로드」섹션에 반영** (파일 수정).\n\n### 이 프로젝트 Git 정보\n\n- remote: `origin` → `https://github.com/jskor013/shealth_bmi_13.git`\n- 일반 브랜치: `prompting` (현재 작업 브랜치 유지. 브랜치 변경 필요 시 사용자에게 확인)\n\n### 커밋 메시지 예\n\n```text\nAdd report and prompt backup for <work-name>\n```\n\n```text\nImplement <feature-or-fix> with report backup\n```\n\n### Git 안전 규칙\n\n- `git config` 변경 금지.\n- `git reset --hard`, **force push**, rebase, amend는 사용자 **명시 요청 시만**.\n- `main`/`master`에 force push 금지.\n- 기존 사용자 변경사항 임의 revert 금지.\n\n---\n\n## 완료 보고 (사용자에게)\n\n1. `report/xx...._report.md` 경로  \n2. `prompting/xx...._prompt.md` 경로  \n3. 커밋 해시·브랜치·push 성공 여부  \n4. 빌드/테스트 결과 (실행한 경우)\n\n---\n\n## 실패 시\n\n| 상황 | 동작 |\n|------|------|\n| 테스트 Red | 3단계 중단, 1·2단계 산출물은 유지 |\n| push 충돌/거부 | force push 없이 원인·해결안 보고 |\n| transcript 없음 | 부모에게 대화 전문 요청; 가능한 범위만 prompting에 기록하고 생략 이유 명시 |\n"
}
```


[Tool: Write]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\.cursor\\agents\\scripts\\export-transcript.py",
  "contents": "#!/usr/bin/env python3\n\"\"\"\nExport Cursor agent-transcript JSONL to prompting markdown (no summarization).\nUsage:\n  python .cursor/agents/scripts/export-transcript.py --number 01 --slug work_name --output prompting/01.work_name_prompt.md\n  python .cursor/agents/scripts/export-transcript.py --transcript path/to/file.jsonl --number 01 --slug work_name\n\"\"\"\nfrom __future__ import annotations\n\nimport argparse\nimport json\nimport re\nimport sys\nfrom datetime import datetime, timezone\nfrom pathlib import Path\n\n\nDEFAULT_TRANSCRIPT_ROOT = Path(\n    r\"C:\\Users\\disab\\.cursor\\projects\\e-dev-shealth-bmi-13\\agent-transcripts\"\n)\n\n\ndef find_latest_jsonl(root: Path) -> Path | None:\n    files = list(root.glob(\"**/*.jsonl\"))\n    if not files:\n        return None\n    return max(files, key=lambda p: p.stat().st_mtime)\n\n\ndef strip_user_query_wrapper(text: str) -> str:\n    m = re.search(r\"<user_query>\\s*(.*?)\\s*</user_query>\", text, re.DOTALL)\n    return m.group(1).strip() if m else text\n\n\ndef content_to_text(content) -> str:\n    if isinstance(content, str):\n        return content\n    if not isinstance(content, list):\n        return str(content)\n    parts: list[str] = []\n    for item in content:\n        if not isinstance(item, dict):\n            parts.append(str(item))\n            continue\n        t = item.get(\"type\")\n        if t == \"text\":\n            parts.append(item.get(\"text\", \"\"))\n        elif t == \"tool_use\":\n            name = item.get(\"name\", \"unknown\")\n            inp = json.dumps(item.get(\"input\", {}), ensure_ascii=False, indent=2)\n            parts.append(f\"\\n[Tool: {name}]\\n```json\\n{inp}\\n```\\n\")\n        else:\n            parts.append(json.dumps(item, ensure_ascii=False, indent=2))\n    return \"\\n\".join(parts)\n\n\ndef parse_jsonl(path: Path) -> list[dict]:\n    turns: list[dict] = []\n    for line in path.read_text(encoding=\"utf-8\").splitlines():\n        line = line.strip()\n        if not line:\n            continue\n        try:\n            turns.append(json.loads(line))\n        except json.JSONDecodeError:\n            turns.append({\"role\": \"raw\", \"raw_line\": line})\n    return turns\n\n\ndef build_markdown(\n    turns: list[dict],\n    work_name: str,\n    report_path: str,\n    transcript_path: Path,\n) -> str:\n    now = datetime.now(timezone.utc).strftime(\"%Y-%m-%dT%H:%M:%SZ\")\n    lines = [\n        f\"# 프롬프트 기록: {work_name}\",\n        \"\",\n        f\"- 보고서 파일: {report_path}\",\n        f\"- Export 일시: {now}\",\n        \"- Export 원칙: 요약 없음, 원문 보존 우선\",\n        f\"- Transcript 출처: {transcript_path}\",\n        \"\",\n        \"## 대화 전문\",\n        \"\",\n    ]\n\n    turn_idx = 0\n    last_role: str | None = None\n\n    for entry in turns:\n        role = entry.get(\"role\", \"unknown\")\n        if role == \"raw\":\n            lines.append(\"### Raw line\")\n            lines.append(\"\")\n            lines.append(f\"```\\n{entry.get('raw_line', '')}\\n```\")\n            lines.append(\"\")\n            continue\n\n        msg = entry.get(\"message\", {})\n        content = msg.get(\"content\") if isinstance(msg, dict) else msg\n        text = content_to_text(content)\n        if role == \"user\":\n            text = strip_user_query_wrapper(text)\n            turn_idx += 1\n            lines.append(f\"### Turn {turn_idx} - User Prompt\")\n        elif role == \"assistant\":\n            label = \"AI Answer\" if last_role != \"assistant\" else \"AI Answer (continued)\"\n            if last_role != \"user\":\n                lines.append(f\"### Turn {turn_idx} - {label}\")\n            else:\n                lines.append(f\"### Turn {turn_idx} - AI Answer\")\n        else:\n            lines.append(f\"### {role}\")\n        lines.append(\"\")\n        lines.append(text)\n        lines.append(\"\")\n        last_role = role\n\n    lines.extend(\n        [\n            \"## 실행 명령 및 도구 출력\",\n            \"\",\n            \"(JSONL 내 Tool 블록 및 셸 출력은 위 Turn에 포함됨)\",\n            \"\",\n            \"## 최종 상태\",\n            \"\",\n            \"- Export: 스크립트 자동 생성 (수동 검토 권장)\",\n            \"\",\n        ]\n    )\n    return \"\\n\".join(lines)\n\n\ndef main() -> int:\n    parser = argparse.ArgumentParser(description=\"Export Cursor transcript to prompting markdown\")\n    parser.add_argument(\"--number\", required=True, help=\"Report number, e.g. 01\")\n    parser.add_argument(\"--slug\", required=True, help=\"Work name slug, e.g. cursorrules_setup\")\n    parser.add_argument(\"--output\", help=\"Output .md path (default: prompting/NN.slug_prompt.md)\")\n    parser.add_argument(\"--transcript\", help=\"Path to .jsonl (default: latest under agent-transcripts)\")\n    parser.add_argument(\"--transcript-root\", default=str(DEFAULT_TRANSCRIPT_ROOT))\n    args = parser.parse_args()\n\n    num = args.number.zfill(2) if args.number.isdigit() else args.number\n    slug = args.slug\n    report_path = f\"report/{num}.{slug}_report.md\"\n    output = Path(args.output or f\"prompting/{num}.{slug}_prompt.md\")\n\n    if args.transcript:\n        transcript_path = Path(args.transcript)\n    else:\n        root = Path(args.transcript_root)\n        transcript_path = find_latest_jsonl(root)\n        if transcript_path is None:\n            print(f\"No .jsonl found under {root}\", file=sys.stderr)\n            return 1\n\n    if not transcript_path.is_file():\n        print(f\"Transcript not found: {transcript_path}\", file=sys.stderr)\n        return 1\n\n    turns = parse_jsonl(transcript_path)\n    md = build_markdown(turns, slug.replace(\"_\", \" \"), report_path, transcript_path)\n    output.parent.mkdir(parents=True, exist_ok=True)\n    output.write_text(md, encoding=\"utf-8\")\n    print(f\"Wrote {output} ({len(turns)} JSONL records from {transcript_path})\")\n    return 0\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())\n"
}
```


[Tool: Write]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\report\\.gitkeep",
  "contents": ""
}
```


[Tool: Write]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\prompting\\.gitkeep",
  "contents": ""
}
```


### Turn 3 - AI Answer (continued)

[REDACTED]

[Tool: Shell]
```json
{
  "command": "cd \"e:\\dev\\shealth_bmi_13\"; python .cursor/agents/scripts/export-transcript.py --number 01 --slug report_backup_agent --output prompting/01.report_backup_agent_prompt.md",
  "description": "Test transcript export script"
}
```


[Tool: Glob]
```json
{
  "glob_pattern": "**/*",
  "target_directory": "e:\\dev\\shealth_bmi_13\\.cursor"
}
```


### Turn 3 - AI Answer (continued)

테스트로 생성된 임시 파일을 삭제합니다.

[REDACTED]

[Tool: Delete]
```json
{
  "path": "e:\\dev\\shealth_bmi_13\\prompting\\01.report_backup_agent_prompt.md"
}
```


### Turn 3 - AI Answer (continued)

`.cursor/agents`에 **보고서·백업·GitHub** 3단계 루틴 Subagent를 생성했습니다.

## 생성된 파일

| 경로 | 설명 |
|------|------|
| `.cursor/agents/report-backup.md` | Subagent 정의 (YAML frontmatter + 3단계 워크플로) |
| `.cursor/agents/scripts/export-transcript.py` | agent-transcripts JSONL → prompting MD 변환 (요약 없음) |
| `report/.gitkeep` | 보고서 저장 폴더 |
| `prompting/.gitkeep` | 대화 Export 저장 폴더 |

## 사용 방법

채팅에서 다음처럼 요청하면 Agent가 **1 → 2 → 3** 순서로 실행합니다.

- `보고서작성 및 백업`
- `보고서 작성 및 백업`
- `/report-backup`

## 3단계 루틴

| 단계 | 산출물 |
|------|--------|
| 1 | `report/01.작업이름_report.md` (번호는 기존 최대+1, 없으면 `01`) |
| 2 | `prompting/01.작업이름_prompt.md` (동일 번호·이름, **요약 없이** 원문) |
| 3 | `git add` → `commit` → `git push` (코드 변경 시 `ctest` Green 확인 후) |

## 2단계 Export

- 부모 Agent가 넘긴 대화 전문을 우선 사용
- 없으면 `agent-transcripts`의 **최신 `.jsonl`** 을 읽음
- 권장 명령:

```powershell
python .cursor/agents/scripts/export-transcript.py --number 01 --slug 작업이름 --output prompting/01.작업이름_prompt.md
```

## Git

- remote: `https://github.com/jskor013/shealth_bmi_13.git`
- `보고서작성 및 백업` 시 commit/push는 승인된 것으로 처리
- 테스트 Red이면 push 중단

첫 백업을 지금 실행하려면 **`보고서작성 및 백업`** 이라고 보내 주세요.

[REDACTED]

### Turn 4 - User Prompt

보고서작성 및 백업

## 실행 명령 및 도구 출력

(JSONL 내 Tool 블록 및 셸 출력은 위 Turn에 포함됨)

## 최종 상태

- Export: 스크립트 자동 생성 (수동 검토 권장)
